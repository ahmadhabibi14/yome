// import { useLocation } from "react-router-dom";

// ---
// const { href, ...props } = Astro.props;
// const { pathname } = Astro.url;
// const currentPath = href === pathname || href === pathname.replace(/\/$/, '');
// ---

// <a href={ href } class:list={["navmenu", { active: currentPath }]} {...props}>
//    <slot />
// </a>